# Database Configuration
DB_HOST=mysql
DB_NAME=yii2advanced
DB_USER=yii2advanced
DB_PASSWORD=secret

# Security Keys (Generate with: php -r "echo bin2hex(random_bytes(32));")
COOKIE_VALIDATION_KEY=
JWT_SECRET=

# Email Configuration
MAIL_HOST=smtp.example.com
MAIL_USERNAME=mail@upsrlm.org
MAIL_PASSWORD=
MAIL_FROM_EMAIL=noreply@upsrlm.org
MAIL_FROM_NAME="UPSRLM Support"

# Application Environment
YII_ENV=dev
YII_DEBUG=1

# SMS Configuration (if applicable)
SMS_LANE_ENABLE=false

# API Configuration
API_LOG_LEVEL=error
API_RESPONSE_FORMAT=json
